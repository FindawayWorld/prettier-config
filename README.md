# `@findaway/prettier-config`

> Findaway Team [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ yarn add --dev @findaway/prettier-config
```

**Edit `package.json`**:

```jsonc
{
    // ...
    "prettier": "@azz/prettier-config"
}
```
